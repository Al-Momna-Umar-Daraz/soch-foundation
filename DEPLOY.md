# SOCH Foundation — free deployment

This project is ready for Render's free web service. Create a new Render Web Service from the GitHub repository, keep the `render.yaml` defaults, and deploy. Render will provide a free `onrender.com` URL. A paid custom domain can be connected later.

For production user accounts, connect a hosted database before launch because free web services do not preserve local filesystem changes.
